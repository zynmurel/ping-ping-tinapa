const PingPingIcon = ({ classStyle }: { classStyle: string }) => {
  return (
    <span className={classStyle}>
      <span className=" -mt-0  text-[#023047] ">Ping</span>
      <span className=" -mt-4 text-[#ffffff]">Ping's</span>
    </span>
  );
};

export default PingPingIcon;
